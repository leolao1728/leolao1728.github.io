---
title: "Black–Scholes–Merton Pricing and Option Strategy Greeks"
description: "Derivation of the Black–Scholes–Merton pricing model and analysis of time-dependent Greek exposures across option strategies."
date: 2026-08-16
categories:
  - Python
  - Option Pricing
  - Options Greeks
  - Option Strategies
body-classes: project-page
---

## Overview

The project derives the risk-neutral distribution of the terminal stock price and uses it to obtain the closed-form pricing formulas for European options. It then examines the time-dependent behaviour of Delta, Gamma, Vega, Theta and Rho under the model.

Building on these theoretical results, the project uses Python to analyse how the aggregate Greek exposures of selected multi-leg option strategies evolve as the underlying asset price and time to maturity change. The analysis connects option-pricing theory with practical strategy construction, visualization and risk management.

## Research Question

How can the Black–Scholes–Merton framework be used to price European options and analyse the changing Greek exposures of multi-leg option strategies?

## Black–Scholes–Merton Framework

In the setting of Black–Scholes-Merton Model, the price of a stock $S_t$ follows

$$
\frac{dS_t}{S_t} = \mu dt + \sigma dW_t
$$

Under the risk-neutral measure, $\mu = r$ and call option price is given by

$$
C=e^{-rT}\mathbb{E}_{\mathbb{Q}}[(S_T-K)^+]
$$

<details>
<summary><strong>Click to see derivation of the distribution of $S_T$</strong></summary>

Using Ito's lemma on $f(S_t)=\ln S_t$,

$$
df=\frac1{S_t}dS_t-\frac12\frac1{S_t^2}(dS_t)^2 =\frac1{S_t}dS_t-\frac12\frac1{S_t^2}S_t^2 \sigma^2 dt  = \frac1{S_t}dS_t-\frac12\sigma^2dt
$$

Integrating from $0$ to $T$,

$$
\ln(S_T)-\ln(S_0)+\frac12\sigma^2T=rT+\sigma W_T
$$

Hence,

$$
S_T=S_0\exp\left((r-\frac12\sigma^2)T+\sigma W_T\right).
$$

Since $W_T=\sqrt{T}Z$ with $Z\sim N(0,1)$,

$$
S_T=S_0\exp\left((r-\frac12\sigma^2)T+\sigma\sqrt{T}Z\right).
$$

</details>

<details>
<summary><strong>Click to see Black-Scholes pricing derivation</strong></summary>

Starting from

$$
C=e^{-rT}\int_{-\infty}^{\infty}\max(S_T-K,0)\phi(z)\,dz
$$

For $S_T>K$,

$$
S_0\exp\left((r-\frac12\sigma^2)T+\sigma\sqrt{T}Z\right) > K \Rightarrow Z > - \frac{\ln{(S_0/K)} + (r-\frac12\sigma^2)T}{\sigma\sqrt{T}} := -d_2
$$

Therefore,

$$
C=e^{-rT}\int_{-d_2}^{\infty}(S_T-K)\phi(z)\,dz = \int_{-d_2}^{\infty} e^{-rT} S_T \phi(z)dz -\int_{-d_2}^{\infty}K e^{-rT} \phi(z)\,dz
$$

After completing the square,

$$
\int_{-d_2}^{\infty}
S_0e^{\frac12\sigma^2T+\sigma\sqrt{T}z}
\phi(z)\,dz
=
S_0N(d_1).
$$

Also,

$$
\int_{-d_2}^{\infty}
Ke^{-rT}\phi(z)\,dz
=
Ke^{-rT}N(d_2).
$$

Hence,

$$
C=S_0N(d_1)-Ke^{-rT}N(d_2).
$$

</details>

### Final Result

$$
\boxed{
C=S_0N(d_1)-Ke^{-rT}N(d_2)
}
$$
where

- 
$$
d_1 =
\frac{
\ln(S_0/K)+(r+\frac{1}{2}\sigma^2)T
}{
\sigma\sqrt{T}
},
$$

- 
$$
d_2=d_1-\sigma\sqrt{T}.
$$

By Put-Call parity $C+K e^{-rT} = P + S_0$, the price of a European put option is $P = C+K e^{-rT} - S_0$

$$
\boxed{
P=Ke^{-rT}N(-d_2)-S_0N(-d_1)
}
$$

## Time-Dependent Greeks
According to the Black-Scholes model, the value of a European call option at time \(t\) is

$$
C(S,t) = S N(d_1) - K e^{-r(T-t)} N(d_2)
$$

where $d_1 =\frac{\ln(S/K)+(r+\frac{1}{2}\sigma^2)(T-t)}{\sigma\sqrt{T-t}},$ and $d_2=d_1-\sigma\sqrt{T-t}.$


### Delta: 
$$\frac{\partial C}{\partial S} = N(d_1) \geq 0 ,\, \frac{\partial P}{\partial S} = N(d_1)-1 \leq 0$$

### Gamma: 
$$\frac{\partial^2 C}{\partial S^2} = \frac{\partial^2 P}{\partial S^2} =  \frac{N'(d_1)}{S \sigma \sqrt{T-t}} \geq 0$$

### Vega: 
$$\frac{\partial C}{\partial \sigma} = S N'(d_1) \sqrt{T-t} \geq 0$$

### Theta: 
$$\frac{\partial C}{\partial t} = -\frac{S N'(d_1) \sigma}{2 \sqrt{T-t}} - r K e^{-r(T-t)} N(d_2) \leq 0$$

### Rho: 
$$\frac{\partial C}{\partial r} = K (T-t) e^{-r(T-t)} N(d_2) \geq 0$$

## Greeks Dynamics Across Option Strategies
In this section, we explore how the aggregate Greek exposures of selected multi-leg option strategies evolve as the underlying asset price and time to maturity change. The analysis is conducted using Python, leveraging libraries such as NumPy, SciPy, and Matplotlib for numerical computations and visualizations.

### Simulation of Stock Price 
As stock price $S_t$ follows geometric Brownian motion, we simulate its evolution over time using the following equation:
$$
S_{t+\Delta t} = S_t \exp\left( (\mu - \frac{1}{2} \sigma^2) \Delta t + \sigma \sqrt{\Delta t} Z_{t+\Delta t} \right)
$$

where $Z_t \sim \mathcal{N}(0,1)$ and $\Delta t = T/n$ for some positive integer $n$.

$$
\Rightarrow S_{k \Delta t} = S_0 \exp\left( (\mu - \frac{1}{2} \sigma^2) k \Delta t + \sigma \sqrt{\Delta t} \sum_{i=1}^k Z_i \right)
$$

<details>
<summary><strong>Click to see derivation of the equation</strong></summary>

Using Ito's lemma on $f(S_t)=\ln S_t$,

$$
df=\frac1{S_t}dS_t-\frac12\frac1{S_t^2}(dS_t)^2 =\frac1{S_t}dS_t-\frac12\frac1{S_t^2}S_t^2 \sigma^2 dt  = \frac1{S_t}dS_t-\frac12\sigma^2dt
$$

Integrating from $s$ to $s+\Delta s$,

$$
\ln(S_{s+\Delta s})-\ln(S_s)+\frac12\sigma^2\Delta s=r\Delta s+\sigma W_{s+\Delta s}-W_s
$$

Hence,

$$
S_{s+\Delta s} = S_s \exp\left( (\mu - \frac{1}{2} \sigma^2) \Delta s + \sigma \mathcal{N}(0,\Delta s) \right)
$$

let $\mathcal{N}(0,\Delta s) = \sqrt{\Delta s} Z_s$, where $Z_s \sim \mathcal{N}(0,1)$, and let $t=s$, we have

$$
S_{t+\Delta t} = S_t \exp\left( (\mu - \frac{1}{2} \sigma^2) \Delta t + \sigma \sqrt{\Delta t} Z_t \right)
$$

</details>



to be continued...

## Assumptions

- The model assumes constant volatility.
- The model assumes a constant risk-free interest rate.
- Transaction costs are excluded.
- The underlying asset price is assumed to follow geometric Brownian motion.

## Repository

The complete implementation will be available on GitHub.